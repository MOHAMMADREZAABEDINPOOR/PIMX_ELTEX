import type { Metadata } from "next";
import { siteConfig } from "@/lib/site-config";
export const metadata: Metadata = { title: "Privacy policy", description: "How PIMX_ELTEX collects, protects, uses, and deletes account and device information.", alternates: { canonical: "/privacy" } };
export default function PrivacyPage() { return <article className="article-shell"><header className="article-header"><span className="eyebrow">Privacy</span><h1>Your data should stay yours.</h1><p>A plain-English overview of what PIMX_ELTEX stores and why.</p></header><div className="article-body"><h2>Account data</h2><p>We store the profile and authentication data required to operate your account, including your selected country and full date of birth to check the minimum sign-up age. You can update your selected country in your profile. New birth dates are encrypted at rest and shown only to authorized administrators in member details. Passwords and one-time codes are stored only as cryptographic hashes; a repeated password is checked during sign-up and not stored.</p><h2>Device & security metadata</h2><p>For account security, we may store country, city or region, browser family, operating system, device category, sign-in dates, and an encrypted IP address. Location is estimated from the connection and may reflect a VPN or proxy instead of the person’s physical location. IP values are encrypted at rest and only available to authorized administrators for security review.</p><h2>Consent-based analytics</h2><p>After you accept analytics cookies, we record page views, approximate location, device information, and time spent on each page. Choosing “Essential only” prevents optional site analytics from being recorded.</p><h2>Your choices</h2><p>You can request a copy of your account data, revoke active sessions, or delete your account.{siteConfig.privacyEmail ? <> Contact <a href={`mailto:${siteConfig.privacyEmail}`}>{siteConfig.privacyEmail}</a> for assistance.</> : " Use the contact page for assistance."}</p></div></article>; }
